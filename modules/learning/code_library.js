// code_library.js — Predefined code snippets and search helper
export const CODE_LIBRARY = {
  "patterns": { javascript: "// Pattern 1: Rectangular Star\\nfunction pattern1(n) {\\n  for (let i = 0; i < n; i++)\\n    console.log('* '.repeat(n).trim());\\n}\\n// Pattern 2: Right Triangle\\nfunction pattern2(n) {\\n  for (let i = 1; i <= n; i++)\\n    console.log('* '.repeat(i).trim());\\n}", cpp: "// Pattern: Rectangular Star\\nvoid pattern1(int n) {\\n  for (int i = 0; i < n; i++) {\\n    for (int j = 0; j < n; j++) cout << \"* \";\\n    cout << endl;\\n  }\\n}\\n// Right Triangle\\nvoid pattern2(int n) {\\n  for (int i = 0; i < n; i++) {\\n    for (int j = 0; j <= i; j++) cout << \"* \";\\n    cout << endl;\\n  }\\n}", java: "// Pattern: Rectangular Star\\nvoid pattern1(int n) {\\n  for (int i = 0; i < n; i++) {\\n    for (int j = 0; j < n; j++) System.out.print(\"* \");\\n    System.out.println();\\n  }\\n}", python: "# Pattern 1: Rectangular Star\\ndef pattern1(n):\\n    for i in range(n):\\n        print('* ' * n)\\n# Pattern 2: Right Triangle\\ndef pattern2(n):\\n    for i in range(1, n+1):\\n        print('* ' * i)" },
  "input output": { javascript: "// Node.js I/O\\nconst readline = require('readline').createInterface({ input: process.stdin, output: process.stdout });\\nreadline.question('Enter a number: ', num => {\\n  console.log('You entered:', num);\\n  readline.close();\\n});", cpp: "#include <iostream>\\nusing namespace std;\\nint main() {\\n    int n; cin >> n;\\n    cout << \"You entered: \" << n << endl;\\n    return 0;\\n}", java: "import java.util.Scanner;\\npublic class Main {\\n    public static void main(String[] args) {\\n        Scanner sc = new Scanner(System.in);\\n        int n = sc.nextInt();\\n        System.out.println(\"You entered: \" + n);\\n    }\\n}", python: "n = int(input('Enter a number: '))\\nprint('You entered:', n)" },
  "data types": { javascript: "let integer = 42;\\nlet decimal = 3.14;\\nlet text = 'hello';\\nlet flag = true;\\nconsole.log(typeof integer, typeof text, typeof flag);", cpp: "#include <iostream>\\nusing namespace std;\\nint main() {\\n    int a = 42; float b = 3.14f; double c = 3.14; char d = 'A'; bool e = true; string f = \"hello\";\\n    cout << a << b << c << d << e << f;\\n    return 0;\\n}", java: "public class DataTypes {\\n    public static void main(String[] args) {\\n        int a = 42; double b = 3.14; char c = 'A'; boolean d = true; String e = \"hello\";\\n        System.out.println(a + \" \" + b + \" \" + c + \" \" + d + \" \" + e);\\n    }\\n}", python: "a = 42; b = 3.14; c = 'A'; d = True; e = 'hello'\\nprint(type(a), type(b), type(d))" },
  "if": { javascript: "function checkAge(age) {\\n    if (age < 18) return 'Minor';\\n    else if (age < 65) return 'Adult';\\n    else return 'Senior';\\n}", cpp: "string checkAge(int age) {\\n    if (age < 18) return \"Minor\";\\n    else if (age < 65) return \"Adult\";\\n    else return \"Senior\";\\n}", java: "public String checkAge(int age) {\\n    if (age < 18) return \"Minor\";\\n    else if (age < 65) return \"Adult\";\\n    else return \"Senior\";\\n}", python: "def check_age(age):\\n    if age < 18: return 'Minor'\\n    elif age < 65: return 'Adult'\\n    else: return 'Senior'" },
  "switch": { javascript: "function getDay(n) {\\n    switch(n) {\\n        case 1: return 'Monday';\\n        case 2: return 'Tuesday';\\n        default: return 'Invalid';\\n    }\\n}", cpp: "string getDay(int n) {\\n    switch(n) {\\n        case 1: return \"Monday\";\\n        case 2: return \"Tuesday\";\\n        default: return \"Invalid\";\\n    }\\n}", java: "public String getDay(int n) {\\n    switch(n) {\\n        case 1: return \"Monday\";\\n        case 2: return \"Tuesday\";\\n        default: return \"Invalid\";\\n    }\\n}", python: "def get_day(n):\\n    match n:\\n        case 1: return 'Monday'\\n        case 2: return 'Tuesday'\\n        case _: return 'Invalid'" },
  "for loop": { javascript: "for (let i = 0; i < 5; i++) console.log(i);\\nconst arr = [1,2,3,4,5];\\nfor (const x of arr) console.log(x);", cpp: "for (int i = 0; i < 5; i++) cout << i << \" \";\\nint arr[] = {1,2,3,4,5};\\nfor (int x : arr) cout << x << \" \";", java: "for (int i = 0; i < 5; i++) System.out.println(i);\\nint[] arr = {1,2,3,4,5};\\nfor (int x : arr) System.out.print(x + \" \");", python: "for i in range(5): print(i)\\narr = [1,2,3,4,5]\\nfor x in arr: print(x)" },
  "while loop": { javascript: "let i = 0;\\nwhile (i < 5) { console.log(i); i++; }", cpp: "int i = 0;\\nwhile (i < 5) { cout << i << \" \"; i++; }", java: "int i = 0;\\nwhile (i < 5) { System.out.println(i); i++; }", python: "i = 0\\nwhile i < 5:\\n    print(i)\\n    i += 1" },
  "function": { javascript: "// Pass by value (primitives)\\nfunction addOne(x) { return x + 1; }\\n// Pass by reference (arrays)\\nfunction push(arr, val) { arr.push(val); }\\nlet nums = [1,2]; push(nums, 3); // [1,2,3]", cpp: "void addOne(int x) { x++; } // value — unchanged\\nvoid addOneRef(int& x) { x++; } // reference — changes original\\nint main() { int a = 5; addOneRef(a); return 0; }", java: "public void modifyPrimitive(int x) { x = 99; } // no effect\\npublic void modifyArray(int[] arr) { arr[0] = 99; } // changes original", python: "def modify_int(x): x = 99  # no effect\\ndef modify_list(lst): lst[0] = 99  # changes original" },
  "count digits": { javascript: "function countDigits(n) {\\n    n = Math.abs(n);\\n    if (n === 0) return 1;\\n    let count = 0;\\n    while (n > 0) { count++; n = Math.floor(n / 10); }\\n    return count;\\n}", cpp: "int countDigits(int n) {\\n    n = abs(n);\\n    int count = 0;\\n    while (n > 0) { count++; n /= 10; }\\n    return count;\\n}", java: "public int countDigits(int n) {\\n    n = Math.abs(n);\\n    int count = 0;\\n    while (n > 0) { count++; n /= 10; }\\n    return count;\\n}", python: "def count_digits(n):\\n    return len(str(abs(n)))" },
  "reverse": { javascript: "function reverseNumber(n) {\\n    let rev = 0, isNeg = n < 0;\\n    n = Math.abs(n);\\n    while (n > 0) { rev = rev * 10 + n % 10; n = Math.floor(n / 10); }\\n    return isNeg ? -rev : rev;\\n}", cpp: "int reverseNumber(int n) {\\n    long rev = 0;\\n    while (n != 0) { rev = rev * 10 + n % 10; n /= 10; }\\n    if (rev > INT_MAX || rev < INT_MIN) return 0;\\n    return (int)rev;\\n}", java: "public int reverse(int n) {\\n    long rev = 0;\\n    while (n != 0) { rev = rev * 10 + n % 10; n /= 10; }\\n    return (rev > Integer.MAX_VALUE || rev < Integer.MIN_VALUE) ? 0 : (int)rev;\\n}", python: "def reverse_number(n):\\n    sign = -1 if n < 0 else 1\\n    rev = int(str(abs(n))[::-1])\\n    return 0 if rev > 2**31 - 1 else sign * rev" },
  "palindrome": { javascript: "function isPalindrome(n) {\\n    if (n < 0) return false;\\n    let orig = n, rev = 0;\\n    while (n > 0) { rev = rev * 10 + n % 10; n = Math.floor(n / 10); }\\n    return orig === rev;\\n}", cpp: "bool isPalindrome(int n) {\\n    if (n < 0) return false;\\n    long orig = n, rev = 0;\\n    int temp = n;\\n    while (temp > 0) { rev = rev * 10 + temp % 10; temp /= 10; }\\n    return orig == rev;\\n}", java: "public boolean isPalindrome(int n) {\\n    if (n < 0) return false;\\n    long orig = n, rev = 0;\\n    while (n > 0) { rev = rev * 10 + n % 10; n /= 10; }\\n    return orig == rev;\\n}", python: "def is_palindrome(n):\\n    return n >= 0 and str(n) == str(n)[::-1]" },
  "gcd": { javascript: "function gcd(a, b) {\\n    while (b !== 0) { [a, b] = [b, a % b]; }\\n    return a;\\n}\\nfunction lcm(a, b) { return (a * b) / gcd(a, b); }", cpp: "int gcd(int a, int b) {\\n    while (b) { int t = b; b = a % b; a = t; }\\n    return a;\\n}", java: "public int gcd(int a, int b) {\\n    return b == 0 ? a : gcd(b, a % b);\\n}", python: "import math\\ndef gcd(a, b):\\n    return math.gcd(a, b)" },
  "armstrong": { javascript: "function isArmstrong(n) {\\n    const d = String(n).length;\\n    return n === [...String(n)].reduce((s,c) => s + Math.pow(+c, d), 0);\\n}", cpp: "bool isArmstrong(int n) {\\n    int orig = n, sum = 0, d = to_string(n).length();\\n    while (n > 0) { int digit = n % 10; sum += pow(digit, d); n /= 10; }\\n    return sum == orig;\\n}", java: "public boolean isArmstrong(int n) {\\n    int orig = n, sum = 0, d = String.valueOf(n).length();\\n    while (n > 0) { int digit = n % 10; sum += (int)Math.pow(digit, d); n /= 10; }\\n    return sum == orig;\\n}", python: "def is_armstrong(n):\\n    d = len(str(n))\\n    return n == sum(int(c)**d for c in str(n))" },
  "divisor": { javascript: "function getDivisors(n) {\\n    const divs = [];\\n    for (let i = 1; i * i <= n; i++) {\\n        if (n % i === 0) { divs.push(i); if (i !== n/i) divs.push(n/i); }\\n    }\\n    return divs.sort((a,b) => a-b);\\n}", cpp: "vector<int> getDivisors(int n) {\\n    vector<int> divs;\\n    for (int i = 1; i * i <= n; i++) {\\n        if (n % i == 0) { divs.push_back(i); if (i != n/i) divs.push_back(n/i); }\\n    }\\n    sort(divs.begin(), divs.end());\\n    return divs;\\n}", java: "public List<Integer> getDivisors(int n) {\\n    List<Integer> divs = new ArrayList<>();\\n    for (int i = 1; i * i <= n; i++) {\\n        if (n % i == 0) { divs.add(i); if (i != n/i) divs.add(n/i); }\\n    }\\n    Collections.sort(divs);\\n    return divs;\\n}", python: "def get_divisors(n):\\n    divs = []\\n    for i in range(1, int(n**0.5)+1):\\n        if n % i == 0:\\n            divs.append(i)\\n            if i != n//i: divs.append(n//i)\\n    return sorted(divs)" },
  "prime": { javascript: "function isPrime(n) {\\n    if (n < 2) return false;\\n    for (let i = 2; i * i <= n; i++)\\n        if (n % i === 0) return false;\\n    return true;\\n}", cpp: "bool isPrime(int n) {\\n    if (n < 2) return false;\\n    for (int i = 2; i * i <= n; i++)\\n        if (n % i == 0) return false;\\n    return true;\\n}", java: "public boolean isPrime(int n) {\\n    if (n < 2) return false;\\n    for (int i = 2; i * i <= n; i++)\\n        if (n % i == 0) return false;\\n    return true;\\n}", python: "def is_prime(n):\\n    if n < 2: return False\\n    for i in range(2, int(n**0.5)+1):\\n        if n % i == 0: return False\\n    return True" },
  "largest element": { javascript: "function largestElement(arr) {\\n    return Math.max(...arr);\\n}", cpp: "int largestElement(vector<int>& arr) {\\n    return *max_element(arr.begin(), arr.end());\\n}", java: "public int largestElement(int[] arr) {\\n    int max = arr[0];\\n    for (int x : arr) if (x > max) max = x;\\n    return max;\\n}", python: "def largest_element(arr):\\n    return max(arr)" },
  "second largest": { javascript: "function secondLargest(arr) {\\n    let first = -Infinity, second = -Infinity;\\n    for (const x of arr) {\\n        if (x > first) { second = first; first = x; }\\n        else if (x > second && x !== first) second = x;\\n    }\\n    return second === -Infinity ? -1 : second;\\n}", cpp: "int secondLargest(vector<int>& arr) {\\n    int first = INT_MIN, second = INT_MIN;\\n    for (int x : arr) {\\n        if (x > first) { second = first; first = x; }\\n        else if (x > second && x != first) second = x;\\n    }\\n    return second == INT_MIN ? -1 : second;\\n}", java: "public int secondLargest(int[] arr) {\\n    int first = Integer.MIN_VALUE, second = Integer.MIN_VALUE;\\n    for (int x : arr) {\\n        if (x > first) { second = first; first = x; }\\n        else if (x > second && x != first) second = x;\\n    }\\n    return second == Integer.MIN_VALUE ? -1 : second;\\n}", python: "def second_largest(arr):\\n    first = second = float('-inf')\\n    for x in arr:\\n        if x > first: second, first = first, x\\n        elif x > second and x != first: second = x\\n    return -1 if second == float('-inf') else second" },
  "sorted": { javascript: "function isSorted(arr) {\\n    for (let i = 1; i < arr.length; i++)\\n        if (arr[i] < arr[i-1]) return false;\\n    return true;\\n}", cpp: "bool isSorted(vector<int>& arr) {\\n    for (int i = 1; i < arr.size(); i++)\\n        if (arr[i] < arr[i-1]) return false;\\n    return true;\\n}", java: "public boolean isSorted(int[] arr) {\\n    for (int i = 1; i < arr.length; i++)\\n        if (arr[i] < arr[i-1]) return false;\\n    return true;\\n}", python: "def is_sorted(arr):\\n    return all(arr[i] <= arr[i+1] for i in range(len(arr)-1))" },
  "duplicate": { javascript: "function removeDuplicates(arr) {\\n    if (!arr.length) return 0;\\n    let i = 0;\\n    for (let j = 1; j < arr.length; j++)\\n        if (arr[j] !== arr[i]) arr[++i] = arr[j];\\n    return i + 1;\\n}", cpp: "int removeDuplicates(vector<int>& arr) {\\n    if (arr.empty()) return 0;\\n    int i = 0;\\n    for (int j = 1; j < arr.size(); j++)\\n        if (arr[j] != arr[i]) arr[++i] = arr[j];\\n    return i + 1;\\n}", java: "public int removeDuplicates(int[] arr) {\\n    if (arr.length == 0) return 0;\\n    int i = 0;\\n    for (let j = 1; j < arr.length; j++)\\n        if (arr[j] != arr[i]) arr[++i] = arr[j];\\n    return i + 1;\\n}", python: "def remove_duplicates(arr):\\n    if not arr: return 0\\n    i = 0\\n    for j in range(1, len(arr)):\\n        if arr[j] != arr[i]: i += 1; arr[i] = arr[j]\\n    return i + 1" },
  "rotate": { javascript: "function rotateLeft(arr, d) {\\n    d %= arr.length;\\n    return [...arr.slice(d), ...arr.slice(0, d)];\\n}", cpp: "void rotateLeft(vector<int>& arr, int d) {\\n    int n = arr.size(); d %= n;\\n    reverse(arr.begin(), arr.begin()+d);\\n    reverse(arr.begin()+d, arr.end());\\n    reverse(arr.begin(), arr.end());\\n}", java: "public void rotateLeft(int[] arr, int d) {\\n    int n = arr.length; d %= n;\\n    reverse(arr, 0, d-1);\\n    reverse(arr, d, n-1);\\n    reverse(arr, 0, n-1);\\n}", python: "def rotate_left(arr, d):\\n    n = len(arr); d %= n\\n    arr[:] = arr[d:] + arr[:d]" },
  "zero": { javascript: "function moveZeros(arr) {\\n    let j = 0;\\n    for (let i = 0; i < arr.length; i++)\\n        if (arr[i] !== 0) [arr[i], arr[j++]] = [arr[j], arr[i]];\\n}", cpp: "void moveZeros(vector<int>& arr) {\\n    int j = 0;\\n    for (int i = 0; i < arr.size(); i++)\\n        if (arr[i] != 0) swap(arr[i], arr[j++]);\\n}", java: "public void moveZeros(int[] arr) {\\n    int j = 0;\\n    for (int i = 0; i < arr.length; i++)\\n        if (arr[i] != 0) { int t = arr[j]; arr[j++] = arr[i]; arr[i] = t; }\\n}", python: "def move_zeros(arr):\\n    j = 0\\n    for i in range(len(arr)):\\n        if arr[i] != 0: arr[i], arr[j] = arr[j], arr[i]; j += 1" },
  "linear search": { javascript: "function linearSearch(arr, target) {\\n    for (let i = 0; i < arr.length; i++)\\n        if (arr[i] === target) return i;\\n    return -1;\\n}", cpp: "int linearSearch(vector<int>& arr, int target) {\\n    for (int i = 0; i < arr.size(); i++)\\n        if (arr[i] == target) return i;\\n    return -1;\\n}", java: "public int linearSearch(int[] arr, int target) {\\n    for (int i = 0; i < arr.length; i++)\\n        if (arr[i] == target) return i;\\n    return -1;\\n}", python: "def linear_search(arr, target):\\n    for i, x in enumerate(arr): return i if x == target else -1\\n    return -1" },
  "union": { javascript: "function findUnion(a, b) {\\n    let i = 0, j = 0, res = [];\\n    while (i < a.length && j < b.length) {\\n        if (a[i] <= b[j]) { if (!res.length || res[res.length-1] !== a[i]) res.push(a[i]); i++; }\\n        else { if (!res.length || res[res.length-1] !== b[j]) res.push(b[j]); j++; }\\n    }\\n    return res;\\n}", cpp: "vector<int> findUnion(vector<int>& a, vector<int>& b) {\\n    vector<int> res; int i=0, j=0;\\n    while (i < a.size() && j < b.size()) {\\n        if (a[i] <= b[j]) { if (res.empty() || res.back() != a[i]) res.push_back(a[i]); i++; }\\n        else { if (res.empty() || res.back() != b[j]) res.push_back(b[j]); j++; }\\n    }\\n    return res;\\n}", java: "public List<Integer> findUnion(int[] a, int[] b) {\\n    List<Integer> res = new ArrayList<>(); int i=0, j=0;\\n    while (i < a.length && j < b.length) {\\n        if (a[i] <= b[j]) { if (res.isEmpty() || res.get(res.size()-1) != a[i]) res.add(a[i]); i++; }\\n        else { if (res.isEmpty() || res.get(res.size()-1) != b[j]) res.add(b[j]); j++; }\\n    }\\n    return res;\\n}", python: "def find_union(a, b):\\n    res, i, j = [], 0, 0\\n    while i < len(a) and j < len(b):\\n        if a[i] <= b[j]:\\n            if not res or res[-1] != a[i]: res.append(a[i]); i += 1\\n        else:\\n            if not res or res[-1] != b[j]: res.append(b[j]); j += 1\\n    return res" },
  "missing": { javascript: "function missingNumber(arr, n) {\\n    return n*(n+1)/2 - arr.reduce((a,b) => a+b, 0);\\n}", cpp: "int missingNumber(vector<int>& arr, int n) {\\n    int expected = n*(n+1)/2, actual = 0;\\n    for (int x : arr) actual += x;\\n    return expected - actual;\\n}", java: "public int missingNumber(int[] arr, int n) {\\n    int expected = n*(n+1)/2, actual = 0;\\n    for (int x : arr) actual += x;\\n    return expected - actual;\\n}", python: "def missing_number(arr, n):\\n    return n*(n+1)//2 - sum(arr)" },
  "consecutive": { javascript: "function maxConsecutiveOnes(nums) {\\n    let max = 0, count = 0;\\n    for (const x of nums) { if (x === 1) max = Math.max(max, ++count); else count = 0; }\\n    return max;\\n}", cpp: "int maxConsecutiveOnes(vector<int>& nums) {\\n    int max_cnt = 0, cnt = 0;\\n    for (int x : nums) { if (x == 1) max_cnt = max(max_cnt, ++cnt); else cnt = 0; }\\n    return max_cnt;\\n}", java: "public int findMaxConsecutiveOnes(int[] nums) {\\n    int max = 0, count = 0;\\n    for (int x : nums) { if (x == 1) max = Math.max(max, ++count); else count = 0; }\\n    return max;\\n}", python: "def max_consecutive_ones(nums):\\n    max_c = count = 0\\n    for x in nums:\\n        if x == 1: count += 1; max_c = max(max_c, count)\\n        else: count = 0\\n    return max_c" },
  "single": { javascript: "function singleNumber(nums) {\\n    return nums.reduce((a,b) => a ^ b, 0);\\n}", cpp: "int singleNumber(vector<int>& nums) {\\n    int xor_sum = 0;\\n    for (int x : nums) xor_sum ^= x;\\n    return xor_sum;\\n}", java: "public int singleNumber(int[] nums) {\\n    int xor = 0;\\n    for (int x : nums) xor ^= x;\\n    return xor;\\n}", python: "def single_number(nums):\\n    from functools import reduce; from operator import xor\\n    return reduce(xor, nums)" },
  "two sum": { javascript: "function twoSum(nums, target) {\\n    const map = new Map();\\n    for (let i = 0; i < nums.length; i++) {\\n        const comp = target - nums[i];\\n        if (map.has(comp)) return [map.get(comp), i];\\n        map.set(nums[i], i);\\n    }\\n    return [];\\n}", cpp: "vector<int> twoSum(vector<int>& nums, int target) {\\n    unordered_map<int,int> mp;\\n    for (int i = 0; i < nums.size(); i++) {\\n        int comp = target - nums[i];\\n        if (mp.count(comp)) return {mp[comp], i};\\n        mp[nums[i]] = i;\\n    }\\n    return {};\\n}", java: "public int[] twoSum(int[] nums, int target) {\\n    Map<Integer,Integer> map = new HashMap<>();\\n    for (int i = 0; i < nums.length; i++) {\\n        int comp = target - nums[i];\\n        if (map.containsKey(comp)) return new int[]{map.get(comp), i};\\n        map.put(nums[i], i);\\n    }\\n    return new int[]{};\\n}", python: "def two_sum(nums, target):\\n    seen = {}\\n    for i, x in enumerate(nums):\\n        comp = target - x\\n        if comp in seen: return [seen[comp], i]\\n        seen[x] = i\\n    return []" },
  "sort 0 1 2": { javascript: "function sortColors(nums) {\\n    let low = 0, mid = 0, high = nums.length - 1;\\n    while (mid <= high) {\\n        if (nums[mid] === 0) { [nums[low],nums[mid]] = [nums[mid],nums[low]]; low++; mid++; }\\n        else if (nums[mid] === 1) mid++;\\n        else { [nums[mid],nums[high]] = [nums[high],nums[mid]]; high--; }\\n    }\\n}", cpp: "void sortColors(vector<int>& nums) {\\n    int low=0, mid=0, high=nums.size()-1;\\n    while (mid <= high) {\\n        if (nums[mid]==0) { swap(nums[low++], nums[mid++]); }\\n        else if (nums[mid]==1) mid++;\\n        else swap(nums[mid], nums[high--]);\\n    }\\n}", java: "public void sortColors(int[] nums) {\\n    int low=0, mid=0, high=nums.length-1;\\n    while (mid <= high) {\\n        if (nums[mid]==0) { int t=nums[low]; nums[low++]=nums[mid]; nums[mid++]=t; }\\n        else if (nums[mid]==1) mid++;\\n        else { int t=nums[mid]; nums[mid]=nums[high]; nums[high--]=t; }\\n    }\\n}", python: "def sort_colors(nums):\\n    low=mid=0; high=len(nums)-1\\n    while mid <= high:\\n        if nums[mid]==0: nums[low],nums[mid]=nums[mid],nums[low]; low+=1; mid+=1\\n        elif nums[mid]==1: mid+=1\\n        else: nums[mid],nums[high]=nums[high],nums[mid]; high-=1" },
  "majority": { javascript: "function majorityElement(nums) {\\n    let count = 0, candidate = null;\\n    for (const num of nums) { if (count===0) candidate=num; count += num===candidate ? 1 : -1; }\\n    return candidate;\\n}", cpp: "int majorityElement(vector<int>& nums) {\\n    int cnt=0, candidate=0;\\n    for (int x : nums) { if (cnt==0) candidate=x; cnt += x==candidate ? 1 : -1; }\\n    return candidate;\\n}", java: "public int majorityElement(int[] nums) {\\n    int cnt=0; Integer candidate=null;\\n    for (int x : nums) { if (cnt==0) candidate=x; cnt += x==candidate ? 1 : -1; }\\n    return candidate;\\n}", python: "def majority_element(nums):\\n    count=0; candidate=None\\n    for x in nums:\\n        if count==0: candidate=x\\n        count += 1 if x==candidate else -1\\n    return candidate" },
  "subarray": { javascript: "function maxSubArray(nums) {\\n    let maxSum = nums[0], cur = nums[0];\\n    for (let i = 1; i < nums.length; i++) { cur = Math.max(nums[i], cur+nums[i]); maxSum = Math.max(maxSum, cur); }\\n    return maxSum;\\n}", cpp: "int maxSubArray(vector<int>& nums) {\\n    int maxSum=nums[0], cur=nums[0];\\n    for (int i=1; i<nums.size(); i++) { cur=max(nums[i], cur+nums[i]); maxSum=max(maxSum,cur); }\\n    return maxSum;\\n}", java: "public int maxSubArray(int[] nums) {\\n    int maxSum=nums[0], cur=nums[0];\\n    for (int i=1; i<nums.length; i++) { cur=Math.max(nums[i], cur+nums[i]); maxSum=Math.max(maxSum,cur); }\\n    return maxSum;\\n}", python: "def max_sub_array(nums):\\n    max_s = cur = nums[0]\\n    for x in nums[1:]:\\n        cur = max(x, cur+x); max_s = max(max_s, cur)\\n    return max_s" },
  "stock": { javascript: "function maxProfit(prices) {\\n    let minP = Infinity, maxP = 0;\\n    for (const p of prices) { minP = Math.min(minP, p); maxP = Math.max(maxP, p-minP); }\\n    return maxP;\\n}", cpp: "int maxProfit(vector<int>& prices) {\\n    int minP=INT_MAX, maxP=0;\\n    for (int p : prices) { minP=min(minP,p); maxP=max(maxP,p-minP); }\\n    return maxP;\\n}", java: "public int maxProfit(int[] prices) {\\n    int minP=Integer.MAX_VALUE, maxP=0;\\n    for (int p : prices) { minP=Math.min(minP,p); maxP=Math.max(maxP,p-minP); }\\n    return maxP;\\n}", python: "def max_profit(prices):\\n    min_p, max_p = float('inf'), 0\\n    for p in prices: min_p=min(min_p,p); max_p=max(max_p,p-min_p)\\n    return max_p" },
  "rearrange": { javascript: "function rearrangeArray(nums) {\\n    const ans = new Array(nums.length); let pos=0, neg=1;\\n    for (const x of nums) { if (x>0) { ans[pos]=x; pos+=2; } else { ans[neg]=x; neg+=2; } }\\n    return ans;\\n}", cpp: "vector<int> rearrangeArray(vector<int>& nums) {\\n    vector<int> ans(nums.size()); int pos=0, neg=1;\\n    for (int x : nums) { if (x>0) { ans[pos]=x; pos+=2; } else { ans[neg]=x; neg+=2; } }\\n    return ans;\\n}", java: "public int[] rearrangeArray(int[] nums) {\\n    int[] ans = new int[nums.length]; int pos=0, neg=1;\\n    for (int x : nums) { if (x>0) { ans[pos]=x; pos+=2; } else { ans[neg]=x; neg+=2; } }\\n    return ans;\\n}", python: "def rearrange_array(nums):\\n    ans=[0]*len(nums); pos=0; neg=1\\n    for x in nums:\\n        if x>0: ans[pos]=x; pos+=2\\n        else: ans[neg]=x; neg+=2\\n    return ans" },
  "permutation": { javascript: "function nextPermutation(nums) {\\n    let i = nums.length-2;\\n    while (i >= 0 && nums[i] >= nums[i+1]) i--;\\n    if (i >= 0) {\\n        let j = nums.length-1;\\n        while (nums[j] <= nums[i]) j--;\\n        [nums[i],nums[j]] = [nums[j],nums[i]];\\n    }\\n    let l=i+1, r=nums.length-1;\\n    while (l<r) { [nums[l],nums[r]] = [nums[r],nums[l]]; l++; r--; }\\n}", cpp: "void nextPermutation(vector<int>& nums) {\\n    next_permutation(nums.begin(), nums.end());\\n}", java: "public void nextPermutation(int[] nums) {\\n    int i=nums.length-2;\\n    while (i>=0 && nums[i]>=nums[i+1]) i--;\\n    if (i>=0) { int j=nums.length-1; while (nums[j]<=nums[i]) j--; int t=nums[i]; nums[i]=nums[j]; nums[j]=t; }\\n    int l=i+1, r=nums.length-1;\\n    while (l<r) { int t=nums[l]; nums[l++]=nums[r]; nums[r--]=t; }\\n}", python: "def next_permutation(nums):\\n    i=len(nums)-2\\n    while i>=0 and nums[i]>=nums[i+1]: i-=1\\n    if i>=0:\\n        j=len(nums)-1\\n        while nums[j]<=nums[i]: j-=1\\n        nums[i],nums[j]=nums[j],nums[i]\\n    nums[i+1:]=reversed(nums[i+1:])" },
  "leader": { javascript: "function findLeaders(arr) {\\n    const leaders=[]; let maxRight=-Infinity;\\n    for (let i=arr.length-1; i>=0; i--) { if (arr[i]>maxRight) { leaders.push(arr[i]); maxRight=arr[i]; } }\\n    return leaders.reverse();\\n}", cpp: "vector<int> findLeaders(vector<int>& arr) {\\n    vector<int> leaders; int maxRight=INT_MIN;\\n    for (int i=arr.size()-1; i>=0; i--) { if (arr[i]>maxRight) { leaders.push_back(arr[i]); maxRight=arr[i]; } }\\n    reverse(leaders.begin(), leaders.end());\\n    return leaders;\\n}", java: "public List<Integer> findLeaders(int[] arr) {\\n    List<Integer> leaders=new ArrayList<>(); int maxRight=Integer.MIN_VALUE;\\n    for (int i=arr.length-1; i>=0; i--) { if (arr[i]>maxRight) { leaders.add(arr[i]); maxRight=arr[i]; } }\\n    Collections.reverse(leaders); return leaders;\\n}", python: "def find_leaders(arr):\\n    leaders=[]; max_r=float('-inf')\\n    for x in reversed(arr):\\n        if x>max_r: leaders.append(x); max_r=x\\n    return leaders[::-1]" },
  "binary search": { javascript: "function binarySearch(arr, target) {\\n    let lo=0, hi=arr.length-1;\\n    while (lo<=hi) {\\n        const mid=(lo+hi)>>1;\\n        if (arr[mid]===target) return mid;\\n        else if (arr[mid]<target) lo=mid+1;\\n        else hi=mid-1;\\n    }\\n    return -1;\\n}", cpp: "int binarySearch(vector<int>& arr, int target) {\\n    int lo=0, hi=arr.size()-1;\\n    while (lo<=hi) {\\n        int mid=lo+(hi-lo)/2;\\n        if (arr[mid]==target) return mid;\\n        else if (arr[mid]<target) lo=mid+1;\\n        else hi=mid-1;\\n    }\\n    return -1;\\n}", java: "public int binarySearch(int[] arr, int target) {\\n    int lo=0, hi=arr.length-1;\\n    while (lo<=hi) {\\n        int mid=lo+(hi-lo)/2;\\n        if (arr[mid]==target) return mid;\\n        else if (arr[mid]<target) lo=mid+1;\\n        else hi=mid-1;\\n    }\\n    return -1;\\n}", python: "def binary_search(arr, target):\\n    lo, hi = 0, len(arr)-1\\n    while lo<=hi:\\n        mid=(lo+hi)//2\\n        if arr[mid]==target: return mid\\n        elif arr[mid]<target: lo=mid+1\\n        else: hi=mid-1\\n    return -1" },
  "bound": { javascript: "function lowerBound(arr, target) {\\n    let lo=0, hi=arr.length;\\n    while (lo<hi) { const mid=(lo+hi)>>1; if (arr[mid]<target) lo=mid+1; else hi=mid; }\\n    return lo;\\n}\\nfunction upperBound(arr, target) {\\n    let lo=0, hi=arr.length;\\n    while (lo<hi) { const mid=(lo+hi)>>1; if (arr[mid]<=target) lo=mid+1; else hi=mid; }\\n    return lo;\\n}", cpp: "int lowerBound(vector<int>& arr, int target) {\\n    return lower_bound(arr.begin(), arr.end(), target) - arr.begin();\\n}\\nint upperBound(vector<int>& arr, int target) {\\n    return upper_bound(arr.begin(), arr.end(), target) - arr.begin();\\n}", java: "public int lowerBound(int[] arr, int target) {\\n    int lo=0, hi=arr.length;\\n    while (lo<hi) { int mid=lo+(hi-lo)/2; if (arr[mid]<target) lo=mid+1; else hi=mid; }\\n    return lo;\\n}", python: "import bisect\\ndef lower_bound(arr, target): return bisect.bisect_left(arr, target)\\ndef upper_bound(arr, target): return bisect.bisect_right(arr, target)" },
  "occurrence": { javascript: "function firstOccurrence(arr, target) {\\n    let lo=0, hi=arr.length-1, ans=-1;\\n    while (lo<=hi) {\\n        const mid=(lo+hi)>>1;\\n        if (arr[mid]===target) { ans=mid; hi=mid-1; }\\n        else if (arr[mid]<target) lo=mid+1;\\n        else hi=mid-1;\\n    }\\n    return ans;\\n}", cpp: "int firstOccurrence(vector<int>& arr, int target) {\\n    int lo=0, hi=arr.size()-1, ans=-1;\\n    while (lo<=hi) {\\n        int mid=lo+(hi-lo)/2;\\n        if (arr[mid]==target) { ans=mid; hi=mid-1; }\\n        else if (arr[mid]<target) lo=mid+1;\\n        else hi=mid-1;\\n    }\\n    return ans;\\n}", java: "public int firstOccurrence(int[] arr, int target) {\\n    int lo=0, hi=arr.length-1, ans=-1;\\n    while (lo<=hi) {\\n        int mid=lo+(hi-lo)/2;\\n        if (arr[mid]==target) { ans=mid; hi=mid-1; }\\n        else if (arr[mid]<target) lo=mid+1;\\n        else hi=mid-1;\\n    }\\n    return ans;\\n}", python: "def first_occurrence(arr, target):\\n    lo, hi, ans = 0, len(arr)-1, -1\\n    while lo<=hi:\\n        mid=(lo+hi)//2\\n        if arr[mid]==target: ans=mid; hi=mid-1\\n        elif arr[mid]<target: lo=mid+1\\n        else: hi=mid-1\\n    return ans" },
  "bubble sort": { javascript: "function bubbleSort(arr) {\\n    for (let i=0; i<arr.length-1; i++)\\n        for (let j=0; j<arr.length-i-1; j++)\\n            if (arr[j]>arr[j+1]) [arr[j],arr[j+1]]=[arr[j+1],arr[j]];\\n    return arr;\\n}", cpp: "void bubbleSort(vector<int>& arr) {\\n    for (int i=0; i<arr.size()-1; i++)\\n        for (int j=0; j<arr.size()-i-1; j++)\\n            if (arr[j]>arr[j+1]) swap(arr[j],arr[j+1]);\\n}", java: "public void bubbleSort(int[] arr) {\\n    for (int i=0; i<arr.length-1; i++)\\n        for (int j=0; j<arr.length-i-1; j++)\\n            if (arr[j]>arr[j+1]) { int t=arr[j]; arr[j]=arr[j+1]; arr[j+1]=t; }\\n}", python: "def bubble_sort(arr):\\n    for i in range(len(arr)-1):\\n        for j in range(len(arr)-i-1):\\n            if arr[j]>arr[j+1]: arr[j],arr[j+1]=arr[j+1],arr[j]\\n    return arr" },
  "selection sort": { javascript: "function selectionSort(arr) {\\n    for (let i=0; i<arr.length-1; i++) {\\n        let minIdx=i;\\n        for (let j=i+1; j<arr.length; j++) if (arr[j]<arr[minIdx]) minIdx=j;\\n        [arr[i],arr[minIdx]]=[arr[minIdx],arr[i]];\\n    }\\n    return arr;\\n}", cpp: "void selectionSort(vector<int>& arr) {\\n    for (int i=0; i<arr.size()-1; i++) {\\n        int minIdx=i;\\n        for (int j=i+1; j<arr.size(); j++) if (arr[j]<arr[minIdx]) minIdx=j;\\n        swap(arr[i],arr[minIdx]);\\n    }\\n}", java: "public void selectionSort(int[] arr) {\\n    for (int i=0; i<arr.length-1; i++) {\\n        int minIdx=i;\\n        for (int j=i+1; j<arr.length; j++) if (arr[j]<arr[minIdx]) minIdx=j;\\n        int t=arr[i]; arr[i]=arr[minIdx]; arr[minIdx]=t;\\n    }\\n}", python: "def selection_sort(arr):\\n    for i in range(len(arr)-1):\\n        min_idx=min(range(i,len(arr)), key=lambda x: arr[x])\\n        arr[i],arr[min_idx]=arr[min_idx],arr[i]\\n    return arr" },
  "insertion sort": { javascript: "function insertionSort(arr) {\\n    for (let i=1; i<arr.length; i++) {\\n        const key=arr[i]; let j=i-1;\\n        while (j>=0 && arr[j]>key) { arr[j+1]=arr[j]; j--; }\\n        arr[j+1]=key;\\n    }\\n    return arr;\\n}", cpp: "void insertionSort(vector<int>& arr) {\\n    for (int i=1; i<arr.size(); i++) {\\n        int key=arr[i], j=i-1;\\n        while (j>=0 && arr[j]>key) { arr[j+1]=arr[j]; j--; }\\n        arr[j+1]=key;\\n    }\\n}", java: "public void insertionSort(int[] arr) {\\n    for (int i=1; i<arr.length; i++) {\\n        int key=arr[i], j=i-1;\\n        while (j>=0 && arr[j]>key) { arr[j+1]=arr[j]; j--; }\\n        arr[j+1]=key;\\n    }\\n}", python: "def insertion_sort(arr):\\n    for i in range(1, len(arr)):\\n        key=arr[i]; j=i-1\\n        while j>=0 and arr[j]>key: arr[j+1]=arr[j]; j-=1\\n        arr[j+1]=key\\n    return arr" },
  "merge sort": { javascript: "function mergeSort(arr) {\\n    if (arr.length<=1) return arr;\\n    const mid=arr.length>>1;\\n    return merge(mergeSort(arr.slice(0,mid)), mergeSort(arr.slice(mid)));\\n}\\nfunction merge(l,r) {\\n    const res=[]; let i=0,j=0;\\n    while (i<l.length && j<r.length) res.push(l[i]<=r[j] ? l[i++] : r[j++]);\\n    return res.concat(l.slice(i)).concat(r.slice(j));\\n}", cpp: "void mergeSort(vector<int>& arr, int l, int r) {\\n    if (l>=r) return;\\n    int m=(l+r)/2;\\n    mergeSort(arr,l,m); mergeSort(arr,m+1,r);\\n    // merge logic...\\n}", java: "public void mergeSort(int[] arr, int l, int r) {\\n    if (l>=r) return;\\n    int m=(l+r)/2;\\n    mergeSort(arr,l,m); mergeSort(arr,m+1,r);\\n    merge(arr,l,m,r);\\n}", python: "def merge_sort(arr):\\n    if len(arr)<=1: return arr\\n    mid=len(arr)//2\\n    left=merge_sort(arr[:mid]); right=merge_sort(arr[mid:])\\n    return sorted(left+right)" },
  "quick sort": { javascript: "function quickSort(arr, lo=0, hi=arr.length-1) {\\n    if (lo<hi) {\\n        const p=partition(arr,lo,hi);\\n        quickSort(arr,lo,p-1); quickSort(arr,p+1,hi);\\n    }\\n    return arr;\\n}\\nfunction partition(arr,lo,hi) {\\n    const pivot=arr[hi]; let i=lo-1;\\n    for (let j=lo; j<hi; j++) if (arr[j]<=pivot) { i++; [arr[i],arr[j]]=[arr[j],arr[i]]; }\\n    [arr[i+1],arr[hi]]=[arr[hi],arr[i+1]];\\n    return i+1;\\n}", cpp: "void quickSort(vector<int>& arr, int lo, int hi) {\\n    if (lo<hi) {\\n        int p=partition(arr,lo,hi);\\n        quickSort(arr,lo,p-1); quickSort(arr,p+1,hi);\\n    }\\n}", java: "public void quickSort(int[] arr, int lo, int hi) {\\n    if (lo<hi) {\\n        int p=partition(arr,lo,hi);\\n        quickSort(arr,lo,p-1); quickSort(arr,p+1,hi);\\n    }\\n}", python: "def quick_sort(arr, lo=0, hi=None):\\n    if hi is None: hi=len(arr)-1\\n    if lo<hi:\\n        p=partition(arr,lo,hi)\\n        quick_sort(arr,lo,p-1); quick_sort(arr,p+1,hi)\\n    return arr" },
  "linked list": { javascript: "class ListNode { constructor(val) { this.val=val; this.next=null; } }\\nfunction reverseList(head) {\\n    let prev=null, curr=head;\\n    while (curr) { const next=curr.next; curr.next=prev; prev=curr; curr=next; }\\n    return prev;\\n}", cpp: "ListNode* reverseList(ListNode* head) {\\n    ListNode* prev=nullptr;\\n    while (head) { ListNode* next=head->next; head->next=prev; prev=head; head=next; }\\n    return prev;\\n}", java: "public ListNode reverseList(ListNode head) {\\n    ListNode prev=null;\\n    while (head!=null) { ListNode next=head.next; head.next=prev; prev=head; head=next; }\\n    return prev;\\n}", python: "def reverse_list(head):\\n    prev=None\\n    while head: nxt=head.next; head.next=prev; prev=head; head=nxt\\n    return prev" },
  "cycle": { javascript: "function hasCycle(head) {\\n    let slow=head, fast=head;\\n    while (fast && fast.next) { slow=slow.next; fast=fast.next.next; if (slow===fast) return true; }\\n    return false;\\n}", cpp: "bool hasCycle(ListNode* head) {\\n    ListNode *slow=head, *fast=head;\\n    while (fast && fast->next) { slow=slow->next; fast=fast->next->next; if (slow==fast) return true; }\\n    return false;\\n}", java: "public boolean hasCycle(ListNode head) {\\n    ListNode slow=head, fast=head;\\n    while (fast!=null && fast.next!=null) { slow=slow.next; fast=fast.next.next; if (slow==fast) return true; }\\n    return false;\\n}", python: "def has_cycle(head):\\n    slow=fast=head\\n    while fast and fast.next: slow=slow.next; fast=fast.next.next\\n    if slow is fast: return True\\n    return False" },
  "tree": { javascript: "function inorder(root) {\\n    if (!root) return [];\\n    return [...inorder(root.left), root.val, ...inorder(root.right)];\\n}\\nfunction maxDepth(root) {\\n    if (!root) return 0;\\n    return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));\\n}", cpp: "void inorder(TreeNode* root, vector<int>& res) {\\n    if (!root) return;\\n    inorder(root->left, res); res.push_back(root->val); inorder(root->right, res);\\n}\\nint maxDepth(TreeNode* root) {\\n    if (!root) return 0;\\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\\n}", java: "public List<Integer> inorder(TreeNode root) {\\n    List<Integer> res=new ArrayList<>();\\n    inorderHelper(root, res); return res;\\n}\\nprivate void inorderHelper(TreeNode node, List<Integer> res) {\\n    if (node==null) return;\\n    inorderHelper(node.left, res); res.add(node.val); inorderHelper(node.right, res);\\n}", python: "def inorder(root):\\n    return inorder(root.left)+[root.val]+inorder(root.right) if root else []\\ndef max_depth(root):\\n    return 1+max(max_depth(root.left), max_depth(root.right)) if root else 0" },
  "bfs": { javascript: "function bfs(graph, start) {\\n    const visited=new Set([start]), queue=[start], order=[];\\n    while (queue.length) {\\n        const node=queue.shift(); order.push(node);\\n        for (const nb of (graph[node]||[])) { if (!visited.has(nb)) { visited.add(nb); queue.push(nb); } }\\n    }\\n    return order;\\n}", cpp: "vector<int> bfs(vector<vector<int>>& adj, int start) {\\n    vector<bool> vis(adj.size(),false); queue<int> q; q.push(start); vis[start]=true;\\n    vector<int> order;\\n    while (!q.empty()) { int node=q.front(); q.pop(); order.push_back(node);\\n        for (int nb : adj[node]) if (!vis[nb]) { vis[nb]=true; q.push(nb); } }\\n    return order;\\n}", java: "public List<Integer> bfs(List<List<Integer>> adj, int start) {\\n    boolean[] vis=new boolean[adj.size()]; Queue<Integer> q=new LinkedList<>(); q.add(start); vis[start]=true;\\n    List<Integer> order=new ArrayList<>();\\n    while (!q.isEmpty()) { int node=q.poll(); order.add(node);\\n        for (int nb : adj.get(node)) if (!vis[nb]) { vis[nb]=true; q.add(nb); } }\\n    return order;\\n}", python: "from collections import deque\\ndef bfs(graph, start):\\n    visited={start}; queue=deque([start]); order=[]\\n    while queue:\\n        node=queue.popleft(); order.append(node)\\n        for nb in graph.get(node,[]):\\n            if nb not in visited: visited.add(nb); queue.append(nb)\\n    return order" },
  "dfs": { javascript: "function dfs(graph, start, visited=new Set()) {\\n    visited.add(start); const order=[start];\\n    for (const nb of (graph[start]||[])) if (!visited.has(nb)) order.push(...dfs(graph,nb,visited));\\n    return order;\\n}", cpp: "void dfs(int node, vector<vector<int>>& adj, vector<bool>& vis, vector<int>& order) {\\n    vis[node]=true; order.push_back(node);\\n    for (int nb : adj[node]) if (!vis[nb]) dfs(nb,adj,vis,order);\\n}", java: "public void dfs(int node, List<List<Integer>> adj, boolean[] vis, List<Integer> order) {\\n    vis[node]=true; order.add(node);\\n    for (int nb : adj.get(node)) if (!vis[nb]) dfs(nb,adj,vis,order);\\n}", python: "def dfs(graph, start, visited=None):\\n    if visited is None: visited=set()\\n    visited.add(start); order=[start]\\n    for nb in graph.get(start,[]):\\n        if nb not in visited: order.extend(dfs(graph,nb,visited))\\n    return order" },
  "fibonacci": { javascript: "function fib(n) {\\n    if (n<=1) return n;\\n    let a=0, b=1;\\n    for (let i=2; i<=n; i++) [a,b]=[b,a+b];\\n    return b;\\n}", cpp: "int fib(int n) {\\n    if (n<=1) return n;\\n    int a=0, b=1;\\n    for (int i=2; i<=n; i++) { int c=a+b; a=b; b=c; }\\n    return b;\\n}", java: "public int fib(int n) {\\n    if (n<=1) return n;\\n    int a=0, b=1;\\n    for (int i=2; i<=n; i++) { int c=a+b; a=b; b=c; }\\n    return b;\\n}", python: "def fib(n):\\n    if n<=1: return n\\n    a,b=0,1\\n    for _ in range(2,n+1): a,b=b,a+b\\n    return b" },
  "climb": { javascript: "function climbStairs(n) {\\n    if (n<=2) return n;\\n    let a=1, b=2;\\n    for (let i=3; i<=n; i++) [a,b]=[b,a+b];\\n    return b;\\n}", cpp: "int climbStairs(int n) {\\n    if (n<=2) return n;\\n    int a=1, b=2;\\n    for (int i=3; i<=n; i++) { int c=a+b; a=b; b=c; }\\n    return b;\\n}", java: "public int climbStairs(int n) {\\n    if (n<=2) return n;\\n    int a=1, b=2;\\n    for (int i=3; i<=n; i++) { int c=a+b; a=b; b=c; }\\n    return b;\\n}", python: "def climb_stairs(n):\\n    if n<=2: return n\\n    a,b=1,2\\n    for _ in range(3,n+1): a,b=b,a+b\\n    return b" },
  "coin change": { javascript: "function coinChange(coins, amount) {\\n    const dp=new Array(amount+1).fill(Infinity); dp[0]=0;\\n    for (const c of coins) for (let i=c; i<=amount; i++) dp[i]=Math.min(dp[i], dp[i-c]+1);\\n    return dp[amount]===Infinity ? -1 : dp[amount];\\n}", cpp: "int coinChange(vector<int>& coins, int amount) {\\n    vector<int> dp(amount+1, INT_MAX); dp[0]=0;\\n    for (int c : coins) for (int i=c; i<=amount; i++) if (dp[i-c]!=INT_MAX) dp[i]=min(dp[i],dp[i-c]+1);\\n    return dp[amount]==INT_MAX ? -1 : dp[amount];\\n}", java: "public int coinChange(int[] coins, int amount) {\\n    int[] dp=new int[amount+1]; Arrays.fill(dp,amount+1); dp[0]=0;\\n    for (int c : coins) for (int i=c; i<=amount; i++) dp[i]=Math.min(dp[i],dp[i-c]+1);\\n    return dp[amount]>amount ? -1 : dp[amount];\\n}", python: "def coin_change(coins, amount):\\n    dp=[float('inf')]*(amount+1); dp[0]=0\\n    for c in coins:\\n        for i in range(c, amount+1): dp[i]=min(dp[i], dp[i-c]+1)\\n    return -1 if dp[amount]==float('inf') else dp[amount]" },
  "knapsack": { javascript: "function knapsack(weights, values, W) {\\n    const n=weights.length;\\n    const dp=Array.from({length:n+1}, ()=>new Array(W+1).fill(0));\\n    for (let i=1; i<=n; i++) for (let w=0; w<=W; w++) {\\n        dp[i][w]=dp[i-1][w];\\n        if (weights[i-1]<=w) dp[i][w]=Math.max(dp[i][w], dp[i-1][w-weights[i-1]]+values[i-1]);\\n    }\\n    return dp[n][W];\\n}", cpp: "int knapsack(vector<int>& wt, vector<int>& val, int W) {\\n    int n=wt.size();\\n    vector<vector<int>> dp(n+1, vector<int>(W+1,0));\\n    for (int i=1; i<=n; i++) for (int w=0; w<=W; w++) {\\n        dp[i][w]=dp[i-1][w];\\n        if (wt[i-1]<=w) dp[i][w]=max(dp[i][w], dp[i-1][w-wt[i-1]]+val[i-1]);\\n    }\\n    return dp[n][W];\\n}", java: "public int knapsack(int[] wt, int[] val, int W) {\\n    int n=wt.length;\\n    int[][] dp=new int[n+1][W+1];\\n    for (int i=1; i<=n; i++) for (int w=0; w<=W; w++) {\\n        dp[i][w]=dp[i-1][w];\\n        if (wt[i-1]<=w) dp[i][w]=Math.max(dp[i][w], dp[i-1][w-wt[i-1]]+val[i-1]);\\n    }\\n    return dp[n][W];\\n}", python: "def knapsack(wt, val, W):\\n    n=len(wt); dp=[[0]*(W+1) for _ in range(n+1)]\\n    for i in range(1,n+1):\\n        for w in range(W+1):\\n            dp[i][w]=dp[i-1][w]\\n            if wt[i-1]<=w: dp[i][w]=max(dp[i][w], dp[i-1][w-wt[i-1]]+val[i-1])\\n    return dp[n][W]" },
  "lcs": { javascript: "function lcs(s1, s2) {\\n    const m=s1.length, n=s2.length;\\n    const dp=Array.from({length:m+1}, ()=>new Array(n+1).fill(0));\\n    for (let i=1; i<=m; i++) for (let j=1; j<=n; j++) {\\n        if (s1[i-1]===s2[j-1]) dp[i][j]=dp[i-1][j-1]+1;\\n        else dp[i][j]=Math.max(dp[i-1][j], dp[i][j-1]);\\n    }\\n    return dp[m][n];\\n}", cpp: "int lcs(string& s1, string& s2) {\\n    int m=s1.size(), n=s2.size();\\n    vector<vector<int>> dp(m+1, vector<int>(n+1,0));\\n    for (int i=1; i<=m; i++) for (int j=1; j<=n; j++) {\\n        if (s1[i-1]==s2[j-1]) dp[i][j]=dp[i-1][j-1]+1;\\n        else dp[i][j]=max(dp[i-1][j], dp[i][j-1]);\\n    }\\n    return dp[m][n];\\n}", java: "public int lcs(String s1, String s2) {\\n    int m=s1.length(), n=s2.length();\\n    int[][] dp=new int[m+1][n+1];\\n    for (int i=1; i<=m; i++) for (int j=1; j<=n; j++) {\\n        if (s1.charAt(i-1)==s2.charAt(j-1)) dp[i][j]=dp[i-1][j-1]+1;\\n        else dp[i][j]=Math.max(dp[i-1][j], dp[i][j-1]);\\n    }\\n    return dp[m][n];\\n}", python: "def lcs(s1, s2):\\n    m,n=len(s1),len(s2); dp=[[0]*(n+1) for _ in range(m+1)]\\n    for i in range(1,m+1):\\n        for j in range(1,n+1):\\n            if s1[i-1]==s2[j-1]: dp[i][j]=dp[i-1][j-1]+1\\n            else: dp[i][j]=max(dp[i-1][j], dp[i][j-1])\\n    return dp[m][n]" },
  "parentheses": { javascript: "function isValid(s) {\\n    const stack=[], map={')':'(', ']':'[', '}':'{'};\\n    for (const c of s) { if ('([{'.includes(c)) stack.push(c); else if (stack.pop()!==map[c]) return false; }\\n    return stack.length===0;\\n}", cpp: "bool isValid(string s) {\\n    stack<char> st;\\n    for (char c : s) { if (c=='('||c=='['||c=='{') st.push(c);\\n        else { if (st.empty()) return false; char top=st.top(); st.pop();\\n            if ((c==')'&&top!='(')||(c==']'&&top!='[')||(c=='}'&&top!='{')) return false; } }\\n    return st.empty();\\n}", java: "public boolean isValid(String s) {\\n    Deque<Character> stack=new ArrayDeque<>();\\n    for (char c : s.toCharArray()) { if (c=='('||c=='['||c=='{') stack.push(c);\\n        else { if (stack.isEmpty()) return false; char top=stack.pop();\\n            if ((c==')'&&top!='(')||(c==']'&&top!='[')||(c=='}'&&top!='{')) return false; } }\\n    return stack.isEmpty();\\n}", python: "def is_valid(s):\\n    stack=[]; mapping={')':'(', ']':'[', '}':'{'}\\n    for c in s:\\n        if c in '([{': stack.append(c)\\n        elif not stack or stack[-1]!=mapping[c]: return False\\n        else: stack.pop()\\n    return not stack" },
  "rain water": { javascript: "function trap(height) {\\n    let l=0, r=height.length-1, water=0, maxL=0, maxR=0;\\n    while (l<r) { if (height[l]<=height[r]) { maxL=Math.max(maxL,height[l]); water+=maxL-height[l]; l++; } else { maxR=Math.max(maxR,height[r]); water+=maxR-height[r]; r--; } }\\n    return water;\\n}", cpp: "int trap(vector<int>& h) {\\n    int l=0, r=h.size()-1, water=0, maxL=0, maxR=0;\\n    while (l<r) { if (h[l]<=h[r]) { maxL=max(maxL,h[l]); water+=maxL-h[l]; l++; } else { maxR=max(maxR,h[r]); water+=maxR-h[r]; r--; } }\\n    return water;\\n}", java: "public int trap(int[] h) {\\n    int l=0, r=h.length-1, water=0, maxL=0, maxR=0;\\n    while (l<r) { if (h[l]<=h[r]) { maxL=Math.max(maxL,h[l]); water+=maxL-h[l]; l++; } else { maxR=Math.max(maxR,h[r]); water+=maxR-h[r]; r--; } }\\n    return water;\\n}", python: "def trap(height):\\n    l,r,water,maxL,maxR=0,len(height)-1,0,0,0\\n    while l<r:\\n        if height[l]<=height[r]: maxL=max(maxL,height[l]); water+=maxL-height[l]; l+=1\\n        else: maxR=max(maxR,height[r]); water+=maxR-height[r]; r-=1\\n    return water" },
  "dijkstra": { javascript: "function dijkstra(graph, src, V) {\\n    const dist=new Array(V).fill(Infinity); dist[src]=0;\\n    const pq=[[0,src]];\\n    while (pq.length) { pq.sort((a,b)=>a[0]-b[0]); const [d,u]=pq.shift(); if (d>dist[u]) continue;\\n        for (const [v,w] of (graph[u]||[])) { if (dist[u]+w<dist[v]) { dist[v]=dist[u]+w; pq.push([dist[v],v]); } } }\\n    return dist;\\n}", cpp: "vector<int> dijkstra(vector<vector<pair<int,int>>>& graph, int src, int V) {\\n    vector<int> dist(V,INT_MAX); dist[src]=0;\\n    priority_queue<pair<int,int>,vector<pair<int,int>>,greater<>> pq; pq.push({0,src});\\n    while (!pq.empty()) { auto [d,u]=pq.top(); pq.pop(); if (d>dist[u]) continue;\\n        for (auto [v,w] : graph[u]) if (dist[u]+w<dist[v]) { dist[v]=dist[u]+w; pq.push({dist[v],v}); } }\\n    return dist;\\n}", java: "public int[] dijkstra(List<List<int[]>> graph, int src, int V) {\\n    int[] dist=new int[V]; Arrays.fill(dist,Integer.MAX_VALUE); dist[src]=0;\\n    PriorityQueue<int[]> pq=new PriorityQueue<>((a,b)->a[0]-b[0]); pq.add(new int[]{0,src});\\n    while (!pq.isEmpty()) { int[] curr=pq.poll(); int d=curr[0],u=curr[1]; if (d>dist[u]) continue;\\n        for (int[] edge : graph.get(u)) { int v=edge[0],w=edge[1]; if (dist[u]+w<dist[v]) { dist[v]=dist[u]+w; pq.add(new int[]{dist[v],v}); } } }\\n    return dist;\\n}", python: "import heapq\\ndef dijkstra(graph, src, V):\\n    dist=[float('inf')]*V; dist[src]=0; pq=[(0,src)]\\n    while pq:\\n        d,u=heapq.heappop(pq)\\n        if d>dist[u]: continue\\n        for v,w in graph.get(u,[]):\\n            if dist[u]+w<dist[v]: dist[v]=dist[u]+w; heapq.heappush(pq,(dist[v],v))\\n    return dist" },
  "anagram": { javascript: "function isAnagram(s, t) {\\n    if (s.length!==t.length) return false;\\n    const freq={};\\n    for (const c of s) freq[c]=(freq[c]||0)+1;\\n    for (const c of t) { if (!freq[c]) return false; freq[c]--; }\\n    return true;\\n}", cpp: "bool isAnagram(string s, string t) {\\n    if (s.size()!=t.size()) return false;\\n    unordered_map<char,int> freq;\\n    for (char c : s) freq[c]++;\\n    for (char c : t) { if (--freq[c]<0) return false; }\\n    return true;\\n}", java: "public boolean isAnagram(String s, String t) {\\n    if (s.length()!=t.length()) return false;\\n    int[] freq=new int[26];\\n    for (char c : s.toCharArray()) freq[c-'a']++;\\n    for (char c : t.toCharArray()) if (--freq[c-'a']<0) return false;\\n    return true;\\n}", python: "from collections import Counter\\ndef is_anagram(s, t):\\n    return Counter(s)==Counter(t)" },
};

// Fuzzy lookup
export function getCode(title) {
  const key = title.toLowerCase();
  for (const [k, v] of Object.entries(CODE_LIBRARY)) {
    if (key.includes(k) || k.includes(key)) return v;
  }
  // Check by any word
  const words = key.split(/[\s\/\-\(\)]+/).filter(w => w.length > 3);
  for (const word of words) {
    for (const [k, v] of Object.entries(CODE_LIBRARY)) {
      if (k.includes(word)) return v;
    }
  }
  // Generic fallback
  const safe = title.replace(/[^a-zA-Z0-9]/g, '');
  return {
    javascript: `// ${title}\\nfunction solve${safe}(input) {\\n    // Step 1: Validate input\\n    // Step 2: Apply algorithm\\n    // Step 3: Return result\\n    return null;\\n}`,
    cpp: `// ${title}\\nint solve(int n) {\\n    // TODO: Implement ${title}\\n    return 0;\\n}`,
    java: `// ${title}\\npublic int solve(int n) {\\n    // TODO: Implement ${title}\\n    return 0;\\n}`,
    python: `# ${title}\\ndef solve(n):\\n    # TODO: Implement ${title}\\n    pass`
  };
}
